server:
  port: 8080

spring:
  application:
    name: dynamic-surge-engine

  ssl:
    bundle:
      pem:
        aiven-bundle:
          keystore:
            certificate: classpath:service.cert
            private-key: classpath:service.key
          truststore:
            certificate: classpath:ca.pem

  kafka:
    bootstrap-servers: kafka-serge-serge-engine.c.aivencloud.com:22407
    ssl:
      bundle: aiven-bundle
    properties:
      security.protocol: SSL
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: surge-calculator-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

  datasource:
    url: jdbc:mysql://gateway01.us-east-1.prod.aws.tidbcloud.com:4000/test?ssl=true
    username: 237hEU9rNqZWQPW.root
    password: Vzozp2xikSC314oT
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
